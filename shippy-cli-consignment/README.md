## Shippy CLI
This is the client implementation for the shippy service

## How to setup
- build docker image `docker build -t shippy-cli-consignment .`
- run docker image (start the server first) `docker run shippy-service-consignment`.